[build]
command = "npm install && npm start"
publish = "index.js"


[[redirects]]
  from = "/*" # Redirecionar todas as solicitações
  to = "/index.js" # Redirecionar para o arquivo principal do aplicativo Node.js
  status = 200 # Use o status 200 para evitar erros "não encontrado"


